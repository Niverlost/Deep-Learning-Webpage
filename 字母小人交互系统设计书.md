# 字母小人交互系统设计书

## 1. 系统概述

### 1.1 设计目标
字母小人系统是 Deep-Learning-Webpage 项目的核心交互组件，将标题文字 "Deep Learning" 的每个字母转化为具有独立性格、情绪和交互能力的"字母小人"。系统追求 Apple 级别的精致交互体验，通过物理弹簧动画、眼球追踪、情绪状态机、社交行为等机制，让静态文字变得生动有趣。

### 1.2 核心特性
- **物理弹簧动画**：所有运动基于弹簧物理模型，自然流畅
- **眼球追踪**：瞳孔实时跟随鼠标/触摸位置
- **情绪状态机**：11种情绪状态，支持状态转换
- **社交行为**：字母之间会窃窃私语、眼神交流
- **懒惰行为**：长时间不交互会打哈欠、打瞌睡
- **粒子特效**：点击触发星星、爱心等粒子效果
- **节日装饰**：根据日期自动添加节日主题装饰
- **无障碍支持**：支持减少动画偏好设置
- **60fps 性能保证**：requestAnimationFrame 驱动，自动降频保护

### 1.3 文件结构
```
test/
├── js/
│   └── letter-system.js    # 核心系统代码 (~2188行)
├── css/
│   └── style.css           # 字母小人样式 (~1160行相关样式)
└── index.html              # DOM容器 #letterStage
```

---

## 2. 架构设计

### 2.1 系统架构

```
LetterSystem (主控制器)
├── states[]               # 字母状态数组
├── ParticleEngine         # 粒子引擎
├── EmotionBlender         # 表情混合系统
├── LetterMemory           # 个性化记忆系统
├── SoundEngine            # 声音反馈引擎
├── LetterCustomization    # 字母自定义系统
├── ClickChorus            # 连续点击合唱
├── TimeAwareness          # 时间感知系统
├── ShareScreenshot        # 截图分享功能
└── 交互配置与状态管理
```

### 2.2 状态结构

```javascript
const states = chars.map((el, i) => ({
  el, index: i,
  pupils: el.querySelectorAll('.letter-pupil'),
  eyes: el.querySelectorAll('.letter-eye'),
  body: el.querySelector('.letter-body'),
  shadow: el.querySelector('.letter-shadow'),
  state: 'idle',           // 当前状态
  tx: 0, ty: 0, tr: 0, ts: 1,  // 动画目标值
  cx: 0, cy: 0, cr: 0, cs: 1,  // 当前值
  vx: 0, vy: 0, vr: 0, vs: 0,  // 弹簧速度
  lazyTimer: null,
  socialTimer: null,
  talkingTimer: null,
  emotion: 'neutral',
  pupilScale: 1,
  scaredTriggered: false,
  nightMode: isNightTime,
}));
```

---

## 3. 配置系统

### 3.1 交互开关配置

```javascript
const interactionConfig = {
  lazyActions: true,        // 偷懒动作（总开关）
  socialInteractions: true, // 社交互动（总开关）
  snakeFollow: true,        // 排队跟随
  scaredBounce: true,       // 吓退弹跳
  eyeTracking: true,        // 眼球跟随
  bodyReaction: true,       // 身体反应
  squashStretch: true,      // 按住压扁
  hover: true,              // hover 交互
  clickGaze: true,          // 点击注视
  lazyNodOff: true,         // 打瞌睡
  lazyStretch: true,        // 伸懒腰
  lazyYawn: true,           // 打哈欠
  lazyZoneOut: true,        // 走神
  lazyPeek: true,           // 偷看
  lazyRubEyes: true,        // 揉眼睛
  socialWhisper: true,      // 窃窃私语
  socialEyeContact: true,   // 传递眼神
  socialCelebrate: true,    // 庆祝跳跃
};
```

### 3.2 字母序列定义

```javascript
const LETTER_CONFIGS = [
  { letter: 'D',  key: 'D',  delay: 0 },      // Deep - 绿色
  { letter: 'e',  key: 'e',  delay: 0.08 },   // 橙色
  { letter: 'e',  key: 'e2', delay: 0.16 },   // 粉色
  { letter: 'p',  key: 'p',  delay: 0.24 },   // 紫色
  { letter: ' ',  key: null, delay: 0 },      // 空格
  { letter: 'L',  key: 'L',  delay: 0.40 },   // Learning - 蓝色
  { letter: 'e',  key: 'e3', delay: 0.48 },   // 青色
  { letter: 'a',  key: 'a',  delay: 0.56 },   // 橙色
  { letter: 'r',  key: 'r',  delay: 0.64 },   // 蓝色
  { letter: 'n',  key: 'n',  delay: 0.72 },   // 绿色
  { letter: 'i',  key: 'i',  delay: 0.80 },   // 紫色
  { letter: 'n',  key: 'n2', delay: 0.88 },   // 绿色
  { letter: 'g',  key: 'g',  delay: 0.96 },   // 橙色
];
```

### 3.3 情绪参数定义

```javascript
const EmotionBlender = {
  emotionParams: {
    neutral:  { eyeOpen: 1.0, pupilScale: 1.0, mouthWidth: 1.0, mouthHeight: 0,   bodyTilt: 0,   blush: 0,   eyeRotation: 0 },
    happy:    { eyeOpen: 0.6, pupilScale: 1.1, mouthWidth: 1.3, mouthHeight: 0.8, bodyTilt: 0,   blush: 0.6, eyeRotation: 0 },
    surprised:{ eyeOpen: 1.3, pupilScale: 0.8, mouthWidth: 0.8, mouthHeight: 1.2, bodyTilt: 0,   blush: 0,   eyeRotation: 0 },
    sad:      { eyeOpen: 0.7, pupilScale: 0.9, mouthWidth: 0.7, mouthHeight: 0.3, bodyTilt: 3,   blush: 0,   eyeRotation: 0 },
    scared:   { eyeOpen: 1.4, pupilScale: 0.7, mouthWidth: 0.6, mouthHeight: 0.9, bodyTilt: -2,  blush: 0,   eyeRotation: 0 },
    curious:  { eyeOpen: 1.1, pupilScale: 1.1, mouthWidth: 0.6, mouthHeight: 0.2, bodyTilt: 0,   blush: 0,   eyeRotation: 0 },
    bored:    { eyeOpen: 0.5, pupilScale: 0.6, mouthWidth: 0.5, mouthHeight: 0,   bodyTilt: 3,   blush: 0,   eyeRotation: 3 },
    sleepy:   { eyeOpen: 0.4, pupilScale: 0.8, mouthWidth: 0.8, mouthHeight: 0.5, bodyTilt: 2,   blush: 0,   eyeRotation: 0 },
    excited:  { eyeOpen: 1.2, pupilScale: 1.2, mouthWidth: 1.4, mouthHeight: 1.0, bodyTilt: 0,   blush: 0.4, eyeRotation: 0 },
  }
};
```

---

## 4. 状态机设计

### 4.1 有效状态

```javascript
// 每个字母只有一个活跃状态：'idle' | 'hover' | 'lazy' | 'social' | 'scared'
```

### 4.2 状态转换函数

| 函数 | 说明 |
|------|------|
| `enterHover(s)` | 进入悬停状态：停止idle动画，设置happy情绪，播放声音 |
| `exitHover(s)` | 退出悬停状态：恢复idle状态，设置neutral情绪，启动idle动画 |
| `enterLazy(s, action)` | 进入懒惰状态：停止idle动画，执行指定懒惰动作 |
| `exitLazy(s)` | 退出懒惰状态：清除定时器，唤醒动画，恢复idle |
| `enterSocial(s)` | 进入社交状态：停止idle动画 |
| `exitSocial(s)` | 退出社交状态：恢复idle状态，设置neutral情绪 |
| `enterScared(s)` | 进入受惊状态：停止idle动画 |
| `exitScared(s)` | 退出受惊状态：恢复idle状态，设置neutral情绪 |

### 4.3 状态退出通用函数

```javascript
function exitState(s) {
  switch (s.state) {
    case 'hover':  /* 无需特殊处理 */ break;
    case 'lazy':   trackedClearTimeout(s.lazyTimer); s.lazyTimer = null; break;
    case 'social': trackedClearTimeout(s.socialTimer); s.socialTimer = null; break;
    case 'scared': s.scaredTriggered = false; break;
  }
}
```

---

## 5. DOM结构设计

### 5.1 容器结构

```html
<div class="letter-stage" id="letterStage">
  <!-- 字母角色 (动态生成) -->
  <div class="letter-char" data-letter="D" tabindex="0" role="button" aria-label="字母D，点击或按Enter键查看互动效果">
    <div class="letter-body">
      <span class="letter-text">D</span>
      <div class="letter-eyes" aria-hidden="true">
        <div class="letter-eye"><div class="letter-pupil"></div></div>
        <div class="letter-eye"><div class="letter-pupil"></div></div>
      </div>
      <div class="letter-mouth" aria-hidden="true"></div>
      <div class="letter-blush" aria-hidden="true"></div>
      <div class="letter-blush" aria-hidden="true"></div>
      <div class="letter-arm left" aria-hidden="true"></div>
      <div class="letter-arm right" aria-hidden="true"></div>
    </div>
    <div class="letter-shadow" aria-hidden="true"></div>
    <!-- 节日装饰 (动态添加) -->
    <div class="holiday-hat santa-hat" aria-hidden="true"></div>
    <div class="holiday-lantern" aria-hidden="true"></div>
  </div>
  
  <!-- 空格 -->
  <div class="letter-space"></div>
  
  <!-- 更多字母... -->
</div>

<!-- 粒子画布 (动态添加) -->
<canvas id="particle-canvas" style="position:fixed;top:0;left:0;width:100%;height:100%;pointer-events:none;z-index:100;"></canvas>
```

### 5.2 CSS类说明

| 类名 | 作用 | 层级 |
|------|------|------|
| `.letter-stage` | 根容器，flex布局，perspective 900px | 1 |
| `.letter-char` | 单个字母容器，相对定位 | 2 |
| `.letter-body` | 字母身体，包含文字和面部 | 3 |
| `.letter-text` | 字母文字，font-weight: 900 | 4 |
| `.letter-eyes` | 眼睛容器，绝对定位 | 4 |
| `.letter-eye` | 单只眼睛，白色圆形 | 5 |
| `.letter-pupil` | 瞳孔，深色圆形，CSS变量控制scale | 6 |
| `.letter-mouth` | 嘴巴，伪元素绘制 | 4 |
| `.letter-blush` | 腮红，圆形渐变 | 4 |
| `.letter-arm` | 手臂，小矩形 | 4 |
| `.letter-shadow` | 底部阴影 | 3 |
| `.letter-space` | 空格占位，width: 18px | 2 |

### 5.3 情绪CSS类

情绪通过给 `.letter-char` 添加对应类名实现：

```css
/* 开心 */
.letter-char.happy .letter-mouth::before { /* 微笑弧线 */ }
.letter-char.happy .letter-blush { opacity: 1; }

/* 惊讶 */
.letter-char.surprised .letter-body { transform: rotate(-3deg) translateY(2px); }
.letter-char.surprised .letter-mouth::before { /* O型嘴 */ }
.letter-char.surprised .letter-eye { transform: scale(1.12); }

/* 困倦 */
.letter-char.sleepy .letter-eye { height: 60%; border-radius: 50% 50% 40% 40%; }
.letter-char.sleepy .letter-mouth::before { /* 小圆圈嘴 */ }

/* 打哈欠 */
.letter-char.yawning .letter-mouth::before { /* 大椭圆嘴 */ }
.letter-char.yawning .letter-eye::after { /* 眼泪 */ }

/* 悲伤 */
.letter-char.sad .letter-mouth::before { /* 倒弧线 */ }
.letter-char.sad .letter-body { transform: scaleY(0.95) rotate(2deg); }

/* 好奇 */
.letter-char.curious .letter-mouth::before { /* 小O嘴 */ }
.letter-char.curious .letter-body { transform: rotate(2deg) translateY(-3px); }

/* 无聊 */
.letter-char.bored .letter-eye { /* 半闭眼 */ }
.letter-char.bored .letter-mouth::before { /* 直线嘴 */ }

/* 兴奋 */
.letter-char.excited .letter-blush { opacity: 1; }
.letter-char.excited .letter-pupil { --pupil-scale: 1.2; }

/* 说话 */
.letter-char.talking .letter-mouth::before { /* 波浪嘴 */ }

/* 受惊 */
.letter-char.scared .letter-mouth::before { /* 颤抖嘴 */ }
```

---

## 6. 交互系统设计

### 6.1 鼠标交互

#### 6.1.1 悬停 (Hover)
```
触发: mouseenter
行为:
  1. 如果状态不是idle，先退出当前状态
  2. 进入hover状态
  3. 停止idle动画
  4. 设置情绪 = happy
  5. 播放悬停声音
```

#### 6.1.2 悬停离开 (Hover End)
```
触发: mouseleave
行为:
  1. 退出hover状态
  2. 恢复idle状态
  3. 设置情绪 = neutral
  4. 启动idle动画
  5. 调度懒惰行为
```

#### 6.1.3 点击 (Click/Tap)
```
触发: click / touchstart(短按)
行为:
  1. 记录交互时间
  2. 记录点击记忆
  3. 播放点击声音
  4. 设置情绪 = happy
  5. 弹簧scale目标 = 0.9 (挤压)
  6. 200ms后恢复scale
  7. 如果状态不是hover，800ms后恢复neutral
  8. 注册合唱点击
  9. 其他idle/lazy字母看向被点击的字母
```

#### 6.1.4 双击 (Double Click/Double Tap)
```
触发: dblclick / 300ms内两次tap
行为:
  1. 设置情绪 = excited
  2. 弹簧rotation目标 = 360 (旋转)
  3. 弹簧y目标 = -20 (大跳跃)
  4. 700ms后恢复
  5. 生成粒子特效 (星星x8 + 爱心x3)
```

#### 6.1.5 长按 (Long Press)
```
触发: touchstart持续500ms
行为:
  1. 设置 isLongPress = true
  2. 设置情绪 = surprised
  3. 弹簧scaleX目标 = 1.15
  4. 弹簧scaleY目标 = 0.85
  5. 生成汗滴粒子
```

#### 6.1.6 释放 (Release)
```
触发: mouseup / touchend(长按后)
行为:
  1. 设置情绪 = happy
  2. scaleX目标 = 0.9
  3. scaleY目标 = 1.1
  4. 250ms后恢复
```

### 6.2 键盘交互

```
Enter/Space: 触发点击
Escape:      唤醒并回到idle状态
```

### 6.3 距离感应系统

#### 6.3.1 梯度响应
```
根据鼠标与字母的距离，字母会有不同的反应:

距离 < 80px (近距离):
  - 受惊反应：字母跳开

距离 80-150px (中距离):
  - 好奇反应：字母倾斜看向鼠标

距离 > 150px (远距离):
  - 无反应
```

#### 6.3.2 受惊反应 (Scared)
```
触发条件: 鼠标距离 < 80px
行为:
  1. 进入scared状态
  2. 设置情绪 = scared
  3. 根据鼠标方向计算推力:
     - x目标 = pushDir * 15 (左右推开)
     - y目标 = -12 (跳起)
     - rotation目标 = pushDir * 10 (倾斜)
     - scaleX目标 = 0.85 (变窄)
     - scaleY目标 = 1.15 (变高)
  4. 播放受惊声音
  5. 1000ms后自动恢复
```

### 6.4 蛇形跟随模式 (Snake Following)

```
触发条件: 鼠标速度 > 40px/frame 连续3帧
行为:
  1. 所有字母进入 snake 状态
  2. 每个字母依次计算指向鼠标的方向
  3. 弹簧目标 = 朝向鼠标的方向 * min(距离*0.1, 15)
  4. rotation目标 = (dx / 200) * 10
  5. 最长持续3000ms，然后冷却3000ms
```

---

## 7. 动画系统

### 7.1 弹簧物理

#### 7.1.1 弹簧公式
```javascript
// I1创新：弹簧物理参数
const SPRING_STIFFNESS = 0.15;
const SPRING_DAMPING = 0.75;

function updateSpring(s, dt) {
  const dtSeconds = dt / 1000;
  
  // 位置弹簧
  const ax = -(s.cx - s.tx) * SPRING_STIFFNESS - s.vx * SPRING_DAMPING;
  const ay = -(s.cy - s.ty) * SPRING_STIFFNESS - s.vy * SPRING_DAMPING;
  s.vx += ax * dtSeconds;
  s.vy += ay * dtSeconds;
  s.cx += s.vx * dtSeconds;
  s.cy += s.vy * dtSeconds;
  
  // 旋转弹簧
  const ar = -(s.cr - s.tr) * SPRING_STIFFNESS - s.vr * SPRING_DAMPING;
  s.vr += ar * dtSeconds;
  s.cr += s.vr * dtSeconds;
  
  // 缩放弹簧
  const as = -(s.cs - s.ts) * SPRING_STIFFNESS - s.vs * SPRING_DAMPING;
  s.vs += as * dtSeconds;
  s.cs += s.vs * dtSeconds;
}
```

### 7.2 待机动画

每个字母有独特的待机动画：

| 字母 | 动画名称 | 描述 |
|------|---------|------|
| D | idleD | 轻微上下浮动 |
| e | idleE | 左右摇摆 |
| p | idleP | 旋转晃动 |
| L | idleL | 倾斜摆动 |
| a | idleA | 弹跳效果 |
| r | idleR | 抖动效果 |
| n | idleN | 波浪效果 |
| i | idleI | 脉冲效果 |
| g | idleG | 摇摆效果 |

### 7.3 入场动画

```
阶段1: 首字母(D)带头入场
  animation: letterHeroEntrance 1.0s cubic-bezier(0.34, 1.56, 0.64, 1)

阶段2: 其他字母依次入场
  animation: letterWakeEntrance 0.9s cubic-bezier(0.34, 1.56, 0.64, 1) delay
  delay = index * 0.08s

关键帧:
  letterHeroEntrance: 从下方弹入 + 缩放
  letterWakeEntrance: 从下方弹入，带延迟
```

---

## 8. 粒子系统

### 8.1 粒子类型

```javascript
const ParticleEngine = {
  types: {
    circle:   { draw: (ctx, p) => { /* 圆形 */ } },
    star:     { draw: (ctx, p) => { /* 五角星 */ } },
    heart:    { draw: (ctx, p) => { /* 心形 */ } },
    note:     { symbol: true, symbols: ['♪', '♫', '♬', '♩'] },
    spark:    { draw: (ctx, p) => { /* 十字 */ } },
    snow:     { draw: (ctx, p) => { /* 雪花 */ } },
    confetti: { draw: (ctx, p) => { /* 矩形 */ } }
  }
};
```

### 8.2 粒子触发条件

| 粒子类型 | 触发条件 | 数量 |
|---------|---------|------|
| star | 双击字母 | 8 |
| heart | 双击字母 | 3 |
| note | 合唱模式 | 每个字母2个 |
| spark | 长按字母 | 2 |
| confetti | 每5次访问庆祝 | 20 |

### 8.3 粒子渲染

```
update(dt):
  1. 更新生命值: life--
  2. 如果 life <= 0，移除粒子
  3. 应用重力: vy += gravity
  4. 应用摩擦力: vx *= friction, vy *= friction
  5. 更新位置: x += vx, y += vy
  6. 更新旋转: rotation += rotationSpeed

draw():
  1. 计算透明度: alpha = life / maxLife
  2. 根据shape绘制不同形状
```

---

## 9. 社交行为系统

### 9.1 窃窃私语 (Whisper)

```
触发: 每8秒检查一次
条件:
  1. 两个相邻字母都处于idle状态
  2. 不在冷却期
  3. 25%概率触发

行为:
  1. 两个字母状态变为 social
  2. 设置情绪 = talking
  3. 添加动画: whisperLeft / whisperRight
  4. 3000ms后恢复idle
  5. 设置12秒冷却期
```

### 9.2 眼神交流 (Eye Contact)

```
触发: 每5秒检查一次
条件: 至少2个idle状态的字母

行为:
  1. 随机选择两个idle字母
  2. 字母A看向字母B
  3. 1100ms后，字母B看向字母A
  4. 总共持续2200ms
```

### 9.3 合唱模式 (Chorus)

```
触发: 2秒内点击5次任意字母
行为:
  1. 所有字母依次(间隔50ms)跳跃
  2. 设置情绪 = excited
  3. 添加 .chorus-singing 类
  4. 生成音符粒子
  5. 800ms后落地并恢复
```

---

## 10. 懒惰行为系统

### 10.1 触发条件

```
检查间隔: 每5秒
条件:
  1. 字母处于idle状态
  2. 距离上次交互 > 12秒
  3. 25%概率触发
```

### 10.2 懒惰动作类型

| 动作 | 权重 | 持续时间 | 行为 |
|------|------|---------|------|
| zoneOut (走神) | 0.22 | 3.5-5.5s | 设置bored情绪 |
| nodOff (打瞌睡) | 0.18 | 4.5-7.5s | 设置sleepy，点头动画 |
| peek (偷看) | 0.25 | 2.5-3.5s | 设置curious，旋转 |
| yawn (打哈欠) | 0.15 | 2.8s | 设置yawning，张嘴动画 |
| stretch (伸懒腰) | 0.12 | 2.2s | 添加stretching类，scaleY=1.15 |
| rubEyes (揉眼睛) | 0.08 | 1.8s | 添加rubbing-eyes类 |

### 10.3 唤醒机制

```
唤醒方式:
  1. 鼠标悬停
  2. 点击/触摸
  3. 蛇形跟随触发

唤醒行为:
  1. 清除所有lazy计时器
  2. 停止当前懒惰动作
  3. 弹性唤醒动画
  4. 如果刚睡醒，30%概率揉眼睛
```

---

## 11. 节日装饰系统

### 11.1 支持的节日

| 节日 | 时间范围 | 装饰效果 |
|------|---------|---------|
| 圣诞节 | 12月1日-31日 | 圣诞帽(每3个字母1个)，红绿渐变背景 |
| 新年 | 1月1日-7日 | 金色发光效果，金橙渐变背景 |
| 中秋节 | 9月13日-27日 | 中间字母发光，暖黄渐变背景 |
| 春节 | 1月20日-2月15日 | 红灯笼，红金渐变背景 |

### 11.2 装饰实现

```javascript
function applyHolidayDecorations(stage) {
  const month = new Date().getMonth() + 1;
  const day = new Date().getDate();
  
  // 圣诞节
  if (month === 12) {
    stage.classList.add('holiday-christmas');
    const chars = stage.querySelectorAll('.letter-char');
    chars.forEach((char, i) => {
      if (i % 3 === 0) {
        const hat = document.createElement('div');
        hat.className = 'holiday-hat santa-hat';
        char.appendChild(hat);
      }
    });
  }
  
  // 新年
  if (month === 1 && day <= 7) {
    stage.classList.add('holiday-newyear');
  }
  
  // 中秋节
  if (month === 9 && day >= 13 && day <= 27) {
    stage.classList.add('holiday-midautumn');
  }
  
  // 春节
  if ((month === 1 && day >= 20) || (month === 2 && day <= 15)) {
    stage.classList.add('holiday-spring');
    const chars = stage.querySelectorAll('.letter-char');
    const midIndex = Math.floor(chars.length / 2);
    const lantern = document.createElement('div');
    lantern.className = 'holiday-lantern';
    chars[midIndex].appendChild(lantern);
  }
}
```

---

## 12. 性能优化

### 12.1 渲染优化

```
1. requestAnimationFrame 驱动主循环
2. 位置缓存: getBoundingClientRect 结果缓存100ms
3. 粒子数量控制: 自动清理死亡粒子
4. 减少动画偏好: 检测 prefers-reduced-motion
```

### 12.2 内存管理

```
1. 定时器追踪: 统一管理所有setTimeout/setInterval
2. 销毁函数: 清理所有事件监听、计时器、DOM元素
3. ResizeObserver: 自动清理
4. 页面隐藏时暂停所有动画
```

### 12.3 帧率保护

```
dt限制: Math.min(time - lastTime, 50)  // 最大50ms
避免在后台运行: 检测 isActive 状态
```

---

## 13. 无障碍设计

### 13.1 ARIA属性

```html
<div class="letter-char" 
     data-letter="D" 
     tabindex="0" 
     role="button" 
     aria-label="字母D，点击或按Enter键查看互动效果">
</div>
```

### 13.2 键盘支持

- **Tab**: 焦点导航
- **Enter/Space**: 触发点击
- **Escape**: 唤醒/重置

### 13.3 减少动画支持

```javascript
const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

// 影响:
// - 跳过粒子系统
// - 跳过社交行为
// - 跳过蛇形跟随
// - 简化动画
```

---

## 14. API接口

### 14.1 初始化

```javascript
// 初始化字母小人系统
function initLetterSystem(container) {
  // container: DOM元素，通常是 document.getElementById('letterStage')
}

// 销毁系统
function destroyLetterSystem() {
  if (window.LetterSystem && window.LetterSystem.destroy) {
    window.LetterSystem.destroy();
  }
}
```

### 14.2 全局访问

```javascript
window.LetterSystem = {
  config: interactionConfig,    // 交互配置
  destroy: destroyLetterSystem, // 销毁函数
};

window.ParticleEngine = ParticleEngine;     // 粒子引擎
window.EmotionBlender = EmotionBlender;     // 表情混合
window.SoundEngine = SoundEngine;           // 声音引擎
window.ShareScreenshot = ShareScreenshot;   // 截图分享
```

---

## 15. 颜色设计

### 15.1 字母颜色 (深色主题)

| 字母 | 颜色代码 | 名称 |
|------|---------|------|
| D | #58CC02 | 绿色 |
| e | #FF9600 | 橙色 |
| e2 | #FF4B4B | 红色 |
| p | #CE82FF | 紫色 |
| L | #1CB0F6 | 蓝色 |
| e3 | #00CD7B | 绿色 |
| a | #FFC800 | 黄色 |
| r | #FF4B4B | 红色 |
| n | #7C4DFF | 深紫 |
| i | #00BFA5 | 青色 |
| n2 | #A56E3E | 棕色 |
| g | #CE82FF | 紫色 |

### 15.2 字母颜色 (浅色主题)

| 字母 | 颜色代码 | 名称 |
|------|---------|------|
| D | #4CAF50 | 绿色 |
| e | #FF9800 | 橙色 |
| e2 | #E91E63 | 粉色 |
| p | #9C27B0 | 紫色 |
| L | #2196F3 | 蓝色 |
| e3 | #00BCD4 | 青色 |
| a | #FF5722 | 深橙 |
| r | #3F51B5 | 靛蓝 |
| n | #009688 | 蓝绿 |
| i | #673AB7 | 深紫 |
| n2 | #8BC34A | 浅绿 |
| g | #FF9800 | 橙色 |

---

## 16. 初始化流程

```
1. app.js 检测到 letterStage 容器存在
2. 调用 initLetterSystem(letterStage)
3. initLetterStage() 创建所有字母DOM元素
4. 应用节日装饰
5. 应用字母自定义配置
6. 初始化声音引擎
7. 初始化粒子引擎
8. initLetterSystem(stage) 启动交互系统
   a. 创建状态数组
   b. 绑定事件监听
   c. 启动主循环 (animate)
   d. 启动懒惰检查定时器
   e. 启动社交行为定时器
```

---

## 17. 主循环 (animate)

```
每帧执行 (requestAnimationFrame):

1. 计算 dt (限制最大50ms)
2. 如果暂停或销毁，跳过
3. 更新位置缓存
4. 更新蛇形跟随检测
5. 更新所有弹簧 (updateSpring)
6. 更新瞳孔追踪
7. 更新距离梯度
8. 应用变换 (s.el.style.transform)
9. 更新阴影
10. 请求下一帧
```

---

## 18. 创新功能

### 18.1 I1 - 弹簧物理系统
使用自定义弹簧物理参数（刚度0.15，阻尼0.75）驱动所有动画，自然流畅。

### 18.2 I3 - 瞳孔追踪统一参数
统一的瞳孔追踪算法，所有字母瞳孔同步跟随鼠标。

### 18.3 I7 - 连续点击5次触发合唱
快速点击5次触发所有字母合唱跳跃。

### 18.4 I8 - 深夜模式自动sleepy
22:00-06:00自动进入困倦状态。

### 18.5 I9 - 页面重新可见时集体看向屏幕中心
页面从后台切换回来时，所有字母看向屏幕中心。

### 18.6 I10 - 个性化记忆系统
记录用户点击偏好，每5次访问庆祝。

### 18.7 I11 - 滚动告别动画
字母舞台离开视口时挥手告别。

### 18.8 I12 - 节日装饰系统
根据日期自动添加节日主题装饰。

### 18.9 I20 - 声音反馈预留接口
Web Audio API合成音效（默认静音）。

### 18.10 S1 - 通用粒子系统引擎
Canvas粒子引擎，支持多种粒子类型。

### 18.11 S3 - 表情混合系统
支持两种情绪混合，平滑过渡。

### 18.12 S4 - 时间感知系统
根据时间段调整动画强度和懒惰频率。

### 18.13 S8 - 字母自定义系统
支持自定义字母颜色和配饰。

### 18.14 S9 - 截图分享功能
触发特殊姿势排列，方便截图分享。

---

## 19. 设计原则

### 19.1 物理真实感
- 所有运动基于弹簧物理，而非线性插值
- 质量、刚度、阻尼参数精心调校
- 不同交互有不同的物理特性

### 19.2 情感表达
- 9种情绪状态，每种有独特的视觉表现
- 情绪通过眼睛、嘴巴、身体、腮红综合表达
- 瞳孔缩放增强情绪表现力

### 19.3 社交智能
- 字母之间会相互影响
- 群体行为（合唱、眼神交流）
- 个体行为（懒惰、受惊）

### 19.4 细节丰富
- 眨眼系统（随机间隔、连续眨眼）
- 每个字母有独特的待机动画
- 阴影动态变化
- 粒子特效

### 19.5 性能优先
- 60fps目标
- 智能缓存
- 减少动画偏好支持
- 后台自动暂停

---

*设计书完成日期: 2025年*
*对应代码版本: letter-system.js (~2188行) + style.css (~1160行相关样式)*
*来源: dl-viz-pro 项目适配*
